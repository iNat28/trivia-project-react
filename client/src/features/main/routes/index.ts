import { RouteObject } from 'react-router-dom';
import MainMenu from '../components/MainMenu';

export const mainMenuRoutes: RouteObject[] = [
    {
        path: '',
        Component: MainMenu,
    },
];
